## Sparse Attention

- Motivation: Self-Attention 的时间和空间复杂度过高 (显存和算力消耗大, 无法处理长序列).
    - 时间 $\mathcal{O}(N^2 d)$, 空间 $\mathcal{O}(N^2)$. $N$ 是序列长度, $d$ 是 embedding 维度.

    ![OpenAI 的 Sparse Attention, 其实仅仅是一些 attention pattern.](sparse-attention.png){#fig-sparse-attention}